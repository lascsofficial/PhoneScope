package com.phonescope.inspector.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════
// PhoneScope OLED-Black Color System
// ═══════════════════════════════════════════

// Base surfaces
val Black = Color(0xFF000000)
val Surface = Color(0xFF0A0A0F)
val SurfaceVariant = Color(0xFF12121A)
val SurfaceElevated = Color(0xFF1A1A24)
val PanelGlass = Color(0x08FFFFFF)
val PanelGlassBorder = Color(0x14FFFFFF)

// Primary accent — Electric Cyan
val PrimaryCyan = Color(0xFF00D4FF)
val PrimaryCyanDim = Color(0xFF0099BB)
val PrimaryCyanGlow = Color(0x3300D4FF)

// Secondary accent — Amber
val SecondaryAmber = Color(0xFFFFB300)
val SecondaryAmberDim = Color(0xFFCC8F00)

// Semantic colors
val Success = Color(0xFF00FF87)
val SuccessDim = Color(0xFF00CC6B)
val Warning = Color(0xFFFFB300)
val Error = Color(0xFFFF4444)
val Critical = Color(0xFFFF0055)

// Text hierarchy
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xB3FFFFFF)     // 70% white
val TextTertiary = Color(0x66FFFFFF)      // 40% white
val TextDisabled = Color(0x33FFFFFF)      // 20% white

// Data visualization — score tiers
val TierTitan = Color(0xFFFFD700)         // Gold
val TierFlagship = Color(0xFF00D4FF)      // Cyan
val TierHighEnd = Color(0xFF00FF87)       // Green
val TierMidRange = Color(0xFFFFB300)      // Amber
val TierBudget = Color(0xFFFF8800)        // Orange
val TierLegacy = Color(0xFFFF4444)        // Red

// Chart/graph colors — per-core CPU
val CoreBig0 = Color(0xFFFF4444)
val CoreBig1 = Color(0xFFFF6B6B)
val CoreMid0 = Color(0xFFFFB300)
val CoreMid1 = Color(0xFFFFCC44)
val CoreMid2 = Color(0xFFFFDD66)
val CoreLittle0 = Color(0xFF00D4FF)
val CoreLittle1 = Color(0xFF44DDFF)
val CoreLittle2 = Color(0xFF66E8FF)
val CoreLittle3 = Color(0xFF88EEFF)

// Thermal heatmap gradient
val ThermalCool = Color(0xFF00D4FF)
val ThermalWarm = Color(0xFFFFB300)
val ThermalHot = Color(0xFFFF4444)
val ThermalCritical = Color(0xFFFF0055)

// Security badge colors
val SecurityHardened = Color(0xFF00FF87)
val SecurityGood = Color(0xFF00D4FF)
val SecurityFair = Color(0xFFFFB300)
val SecurityAtRisk = Color(0xFFFF8800)
val SecurityCritical = Color(0xFFFF0055)

// Navigation bar
val NavBarBackground = Color(0xFF050508)
val NavBarIndicator = Color(0x3300D4FF)
